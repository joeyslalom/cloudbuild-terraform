steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'echo'
  args:
    - 'hi cloud build'
# so the service account doesn't need storage roles
options:
  logging: CLOUD_LOGGING_ONLY